<UserControl x:Class="Strokes.GUI.AchievementPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Strokes.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1024">

    <UserControl.Resources>
   
        <local:BoolToImageUriConverter x:Key="ImageConverter"/>
   
        <Style TargetType="TextBlock" >
            <Setter Property="FontFamily" Value="Arial"></Setter>
            <Setter Property="FontSize" Value="12"></Setter>
            <Setter Property="Margin" Value="5,5,5,5"></Setter>
            <Setter Property="Foreground" Value="LightGray"></Setter>
        </Style>

        <Style x:Key="TitleStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontSize" Value="16"></Setter>
            <Setter Property="Foreground" Value="White"></Setter>
        </Style>
        <Style TargetType="Grid" >
            <Setter Property="Background" Value="#FF585858"></Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#FF585858"></Setter>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="#FF585858"></Setter>
            <Setter Property="Margin" Value="2"></Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="BurlyWood"></Setter>
                    
                </Trigger>

            </Style.Triggers>

        </Style>
              
            <DataTemplate x:Key="AchievementTemplate" >
            
                <Border BorderThickness="1" Margin="1" 
                        BorderBrush="Orange" 
                        Background="{Binding Path=Background, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}}}">
                    <Grid  >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition></ColumnDefinition>
                        <ColumnDefinition></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                        <local:AchievementTemplate Grid.Column="0" Margin="5" />
                    <Image Height="20" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="1"  Margin="5,5,5,5"  Width="40" Source="{Binding IsCompleted, Converter={StaticResource ImageConverter}}" />
                </Grid>
                </Border>
           
        </DataTemplate>

        <DataTemplate x:Key="CategoryTemplate">
            <Grid Background="Orange" Margin="0">
                <Border BorderThickness="1" Margin="2" 
                        BorderBrush="Teal" 
                        Background="{Binding Path=Background, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListBoxItem}}}">
                    <Grid Margin="2" >
                        <StackPanel Orientation="Horizontal">
                        <TextBlock HorizontalAlignment="Center"  Text="{Binding CategoryName}" Style="{StaticResource TitleStyle}"/>
                            <TextBlock Text="{Binding TotalCompleted, Mode=OneWay}"></TextBlock>
                            <TextBlock Text="/"></TextBlock>
                            <TextBlock Text="{Binding Count, Mode=OneWay}"></TextBlock>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </DataTemplate>



    </UserControl.Resources>
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition ></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.ColumnSpan="3" Margin="10,10,10,10">
            <TextBlock Style="{StaticResource TitleStyle}">Information page</TextBlock>
            <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5">
                <StackPanel Orientation="Vertical">
                <TextBlock TextWrapping="Wrap" TextAlignment="Center"> Log in to strokes to get your achievements tracked and stuff. </TextBlock>
                    <TextBlock>Summary:</TextBlock>
                    <TextBlock Margin="15">
                        <TextBlock.Inlines>
                            <Run> Total achievements earned: </Run>
                            <Run Text="{Binding TotalCompleted, Mode=OneWay}"></Run>
                            <Run> of </Run>
                            <Run Text="{Binding TotalAchievements, Mode=OneWay}"></Run>
                            <Run> (</Run>
                            <Run Text="{Binding PercentageCompleted, Mode=OneWay}"></Run>
                            <Run> %)</Run>
                        </TextBlock.Inlines>
                    </TextBlock>
                    <ProgressBar Margin="15,0,15,5" Height ="20" Value="{Binding TotalCompleted, Mode=OneWay}" Maximum="{Binding TotalAchievements}" Background="#FF9A9A9A" Foreground="#FF7DF25A"></ProgressBar>
                </StackPanel>
                
            </Border>
        </StackPanel>
        <StackPanel Grid.Column="0" Grid.Row="1" Margin="10,10,10,10">
            <TextBlock Style="{StaticResource TitleStyle}">Achievement Categories</TextBlock>
            <ListBox Margin="5,5,5,5" 
                     x:Name="categroriesLB" 
                     IsSynchronizedWithCurrentItem="true" 
                     ItemsSource="{Binding Path=achievementsOrdered}"
                     ItemTemplate="{StaticResource CategoryTemplate}"
                     >

            </ListBox>
        </StackPanel>


        <ListBox Margin="10,10,10,10" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                 Grid.Row="1" Grid.Column="1" 
                 ItemsSource="{Binding achievementsOrdered/}" 
                 ItemTemplate="{StaticResource AchievementTemplate}">

        </ListBox>

        <StackPanel Grid.Column="2" Grid.Row="1" Margin="10,10,10,10">
            <TextBlock TextWrapping="Wrap">
                Information about what achievements other people you know have been completing.
            </TextBlock>
            <ListBox></ListBox>

        </StackPanel>
        
        
    </Grid>
</UserControl>
