<UserControl x:Class="Strokes.GUI.AchievementPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:template="clr-namespace:Strokes.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.ColumnSpan="3" Margin="10,10,10,10">
            <TextBlock>Information page</TextBlock>
            <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5">
                <TextBlock TextWrapping="Wrap" TextAlignment="Center"> Log in to strokes to get your achievements tracked and stuff. </TextBlock>
            </Border>
        </StackPanel>
        <StackPanel Grid.Column="0" Grid.Row="1" Margin="10,10,10,10">
            <TextBlock>Achievement Categories</TextBlock>
            <ListBox Margin="5,5,5,5">
                <ListBoxItem>Variables (Placeholders: will be autogenerated)</ListBoxItem>
                <ListBoxItem>Collection</ListBoxItem>
            </ListBox>
        </StackPanel>
        <StackPanel Grid.Column="1" Grid.Row="1" Margin="10,10,10,10">
            <ListBox Name="AchievementStack" Grid.Row="1" Grid.Column="1"  ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="#FF585858">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <template:AchievementTemplate Margin="10" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <StackPanel Grid.Column="2" Grid.Row="1" Margin="10,10,10,10">
            <TextBlock TextWrapping="Wrap">Information about what achievements other people you know have been completing.</TextBlock>
            <ListBox></ListBox>

        </StackPanel>
        
        
    </Grid>
</UserControl>
